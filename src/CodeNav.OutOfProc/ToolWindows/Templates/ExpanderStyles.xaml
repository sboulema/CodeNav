<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
                    xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">

    <Style x:Key="ExpanderHeaderFocusVisual">
        <Setter Property="Control.Template">
            <Setter.Value>
                <ControlTemplate>
                    <Border>
                        <Rectangle Margin="0" SnapsToDevicePixels="true" Stroke="Black" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style x:Key="ExpanderDownHeaderStyle" TargetType="ToggleButton">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border Padding="{TemplateBinding Padding}">
                        <vs:Image Source="KnownMonikers.Expand" x:Name="arrow" Width="16" Height="16" Visibility="{Binding Path=HasMembersVisibility}" />
                    </Border>

                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsChecked, RelativeSource={RelativeSource Self}}" Value="True"/>
                                <Condition Binding="{Binding Path=DataContext.HasMembersVisibility, RelativeSource={RelativeSource Self}}" Value="Visible"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Source" TargetName="arrow" Value="KnownMonikers.Collapse" />
                        </MultiDataTrigger>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Source" TargetName="arrow" Value="KnownMonikers.Expand" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <LinearGradientBrush x:Key="ExpanderHeaderGradientBrush" StartPoint="0,0" EndPoint="1,0">
        <GradientStop Color="{DynamicResource {x:Static colors:EnvironmentColors.ComboBoxBorderColorKey}}" Offset="0.0" />
        <GradientStop Color="Transparent" Offset="1" />
    </LinearGradientBrush>
    
    <Style TargetType="Expander">
        <Setter Property="Foreground" Value="{DynamicResource {x:Static colors:EnvironmentColors.ToolWindowTextBrushKey}}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
        <Setter Property="VerticalContentAlignment" Value="Stretch"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Expander">
                    <Border
                        BorderThickness="{TemplateBinding BorderThickness}"
                        Background="{TemplateBinding Background}"
                        CornerRadius="0"
                        SnapsToDevicePixels="true">

                        <Border.BorderBrush>
                            <VisualBrush>
                                <VisualBrush.Visual>
                                    <Rectangle
                                        x:Name="ExpanderBorderBrush"
                                        StrokeThickness="1"
                                        Width="{Binding RelativeSource={RelativeSource AncestorType={x:Type Expander}}, Path=ActualWidth}"
                                        Height="{Binding RelativeSource={RelativeSource AncestorType={x:Type Expander}}, Path=ActualHeight}" />
                                </VisualBrush.Visual>
                            </VisualBrush>
                        </Border.BorderBrush>

                        <StackPanel Orientation="Vertical">
                            <Grid Background="Transparent" SnapsToDevicePixels="False" Margin="0,0,4,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="17"/>
                                </Grid.ColumnDefinitions>

                                <Border x:Name="ExpanderHeaderBorder"
                                    BorderThickness="0"
                                    BorderBrush="{StaticResource ExpanderHeaderGradientBrush}">
                                    <Button Command="{Binding Path=ClickItemCommand}" 
                                        ContentTemplate="{TemplateBinding HeaderTemplate}"
                                        ContentTemplateSelector="{TemplateBinding HeaderTemplateSelector}"
                                        Content="{TemplateBinding Header}"
                                        Background="Transparent"
                                        Foreground="{TemplateBinding Foreground}"
                                        FontWeight="{TemplateBinding FontWeight}"
                                        FontStyle="{TemplateBinding FontStyle}"
                                        FontStretch="{TemplateBinding FontStretch}"
                                        FontSize="{TemplateBinding FontSize}"
                                        FontFamily="{TemplateBinding FontFamily}"
                                        HorizontalContentAlignment="Left"
                                        HorizontalAlignment="Stretch"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        FocusVisualStyle="{StaticResource ExpanderHeaderFocusVisual}"
                                        DockPanel.Dock="Top"
                                        BorderThickness="0"
                                        Padding="3,3"
                                        ContextMenu="{DynamicResource ItemContextMenu}"
                                        ToolTip="{Binding Tooltip}">
                                        <Button.InputBindings>
                                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding Path=ToggleExpandCollapseCommand}" />
                                        </Button.InputBindings>
                                    </Button>
                                </Border>

                                <ToggleButton 
                                    Grid.Column="1" 
                                    x:Name="HeaderSite" 
                                    IsChecked="{Binding IsExpanded, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" 
                                    Margin="1" 
                                    MinWidth="0" 
                                    MinHeight="0" 
                                    Style="{StaticResource ExpanderDownHeaderStyle}" 
                                    VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}" />
                            </Grid>
                            <ContentPresenter 
                                x:Name="ExpandSite" 
                                DockPanel.Dock="Bottom" 
                                Focusable="false" 
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                Margin="{TemplateBinding Padding}" 
                                Visibility="Collapsed" />
                        </StackPanel>
                    </Border>
                    <ControlTemplate.Triggers>
                        <MultiDataTrigger>
                            <MultiDataTrigger.Conditions>
                                <Condition Binding="{Binding Path=IsExpanded, RelativeSource={RelativeSource Self}}" Value="True"/>
                                <Condition Binding="{Binding Path=DataContext.HasMembersVisibility, RelativeSource={RelativeSource Self}}" Value="Visible"/>
                            </MultiDataTrigger.Conditions>
                            <Setter Property="Visibility" TargetName="ExpandSite" Value="Visible"/>
                            <Setter Property="BorderThickness" TargetName="ExpanderHeaderBorder" Value="0,0,0,1" />
                        </MultiDataTrigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.GrayTextBrushKey}}"/>
                        </Trigger>
                        <Trigger Property="IsExpanded" Value="False">
                            <Setter Property="BorderThickness" TargetName="ExpanderHeaderBorder" Value="0" />
                        </Trigger>
                        <DataTrigger Binding="{Binding Path=DataContext.IsHighlighted, RelativeSource={RelativeSource Self}}" Value="False">
                            <Setter Property="Stroke" TargetName="ExpanderBorderBrush" Value="{DynamicResource {x:Static styles:VsBrushes.ComboBoxBorderKey}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=DataContext.IsHighlighted, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter Property="Stroke" TargetName="ExpanderBorderBrush" Value="{DynamicResource {x:Static styles:VsBrushes.ClassDesignerDefaultConnectionKey}}" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Path=DataContext.DataTemplateType, RelativeSource={RelativeSource Self}}" Value="Region">
                            <Setter Property="StrokeDashArray" TargetName="ExpanderBorderBrush" Value="5 3" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
