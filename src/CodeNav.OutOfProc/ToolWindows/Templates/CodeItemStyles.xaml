<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:vs="http://schemas.microsoft.com/visualstudio/extensibility/2022/xaml"
                    xmlns:styles="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0"
                    xmlns:colors="clr-namespace:Microsoft.VisualStudio.PlatformUI;assembly=Microsoft.VisualStudio.Shell.15.0">

    <Style x:Key="CodeItemButtonStyle" TargetType="Button">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Padding="{TemplateBinding Padding}">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="{DynamicResource {x:Static styles:VsBrushes.AccentMediumKey}}"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid Background="Transparent">
                            <ContentPresenter></ContentPresenter>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <DataTrigger Binding="{Binding Path=DataContext.IsItalic, RelativeSource={RelativeSource Self}}" Value="True">
                            <Setter Property="FontStyle" Value="Italic" />
                        </DataTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ContentControl">
        <!-- CodeItem Template -->
        <Setter Property="ContentTemplate">
            <Setter.Value>
                <DataTemplate>
                    <Button x:Name="CodeItemButton"
                            ToolTip="{Binding Tooltip}"
                            Visibility="{Binding Visibility}"
                            ContextMenu="{DynamicResource ItemContextMenu}"
                            Opacity="{Binding Opacity}"
                            Style="{StaticResource CodeItemButtonStyle}"
                            FontSize="{Binding FontSize}"
                            Background="Transparent"
                            BorderThickness="0" 
                            HorizontalContentAlignment="Stretch"
                            HorizontalAlignment="Stretch"
                            Margin="-3 -3 -3 0"
                            Padding="3">
                        <Button.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding ClickItemCommand}" />
                            <MouseBinding Gesture="LeftDoubleClick" Command="{Binding DoubleClickItemCommand}" />
                        </Button.InputBindings>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Grid Margin="0 0 3 0" Width="16" Height="16">
                                <vs:Image Source="{Binding Moniker}" />
                                <vs:Image Source="{Binding OverlayMoniker}" Width="8" Height="8" HorizontalAlignment="Right" VerticalAlignment="Bottom" />
                            </Grid>

                            <TextBlock Grid.Column="1">
                                <Run Text="{Binding Name}"       
                                    Style="{DynamicResource HighlightNameStyle}" />
                                <Run Text="{Binding ReturnType}"
                                    Foreground="DarkGray" />
                                <Run Text="{Binding Parameters}"
                                    Foreground="DarkGray" />
                            </TextBlock>

                            <vs:Image 
                                Grid.Column="2"
                                Source="KnownMonikers.History"
                                Margin="3 0 0 0"
                                Width="10"
                                Height="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding StatusMonikerVisibility}" 
								Grayscale="{Binding StatusGrayscale}"
                                Opacity="{Binding StatusOpacity}" />

                            <vs:Image 
                                Grid.Column="3"
                                Source="KnownMonikers.Bookmark"
                                Margin="3 0 0 0"
                                Width="10"
                                Height="10"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding BookmarkVisibility}" />
                        </Grid>
                    </Button>
                    <DataTemplate.Triggers>
                        <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                            <Setter Property="Padding" TargetName="CodeItemButton" Value="3 0" />
                            <Setter Property="Margin" TargetName="CodeItemButton" Value="-3 0" />
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <!-- Namespace Template -->
            <DataTrigger Binding="{Binding DataTemplateType}" Value="Namespace">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <StackPanel>
                                <!-- Show namespace as expander when not ignored -->
                                <Expander
                                    IsExpanded="{Binding IsExpanded}"
                                    Visibility="{Binding IgnoreVisibility}"
                                    Opacity="{Binding Opacity}"
                                    HorizontalAlignment="Stretch">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal">
                                            <vs:Image
                                                Source="{Binding Moniker}"
                                                Width="16"
                                                Height="16"
                                                Margin="0,0,3,0"
                                                VerticalAlignment="Center" />
                                            <TextBlock
                                                FontSize="{Binding FontSize}"
                                                VerticalAlignment="Center">
                                                    <Run Text="{Binding Name}" 
                                                         Style="{DynamicResource HighlightNameStyle}"/>
                                                    <Run Text="{Binding Parameters}" 
                                                         Foreground="DarkGray" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Expander.Header>
                                    <ItemsControl
                                        x:Name="MembersItemsControl"
                                        ItemsSource="{Binding Members}"
                                        Margin="3"
                                        VirtualizingPanel.IsVirtualizing="True"
                                        VirtualizingPanel.VirtualizationMode="Recycling"
                                        ScrollViewer.CanContentScroll="False">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <ContentControl Content="{Binding}">
                                                </ContentControl>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style>
                                                <Setter Property="FrameworkElement.Visibility" Value="{Binding Visibility}"/>
                                                <Setter Property="FrameworkElement.Margin" Value="0 0 0 3"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                        <Setter Property="FrameworkElement.Margin" Value="0 0 0 1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                        <Setter Property="FrameworkElement.Margin" Value="3 0" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ItemsControl.Style>
                                    </ItemsControl>
                                </Expander>

                                <!-- Show namespace as list of its members if ignored -->
                                <ItemsControl 
                                    ItemsSource="{Binding Members}"
                                    Visibility="{Binding NotIgnoreVisibility}"
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.VirtualizationMode="Recycling"
                                    ScrollViewer.CanContentScroll="False">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <ContentControl Content="{Binding}">
                                            </ContentControl>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style>
                                            <Setter Property="FrameworkElement.Visibility" Value="{Binding Visibility}"/>
                                            <Setter Property="FrameworkElement.Margin" Value="0 0 0 3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                    <Setter Property="FrameworkElement.Margin" Value="0 0 0 1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                </ItemsControl>
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                    <Setter Property="Margin" TargetName="MembersItemsControl" Value="3 0" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <!-- Class Template -->
            <DataTrigger Binding="{Binding DataTemplateType}" Value="Class">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <StackPanel>
                                <Expander
                                    IsExpanded="{Binding IsExpanded}"
                                    Opacity="{Binding Opacity}"
                                    HorizontalAlignment="Stretch">
                                    <Expander.Header>
                                        <StackPanel Orientation="Horizontal">
                                            <vs:Image
                                                Source="{Binding Moniker}"
                                                Width="16"
                                                Height="16"
                                                Margin="0 0 3 0"
                                                VerticalAlignment="Center" />
                                            <TextBlock
                                                FontSize="{Binding FontSize}"
                                                VerticalAlignment="Center">
                                                <Run Text="{Binding Name}" 
                                                    Style="{DynamicResource HighlightNameStyle}" />
                                                <Run Text="{Binding Parameters}" 
                                                    Foreground="DarkGray" />
                                            </TextBlock>
                                        </StackPanel>
                                    </Expander.Header>
                                    <ItemsControl
                                        x:Name="MembersItemsControl"
                                        ItemsSource="{Binding Members}"
                                        Margin="3"
                                        VirtualizingPanel.IsVirtualizing="True"
                                        VirtualizingPanel.VirtualizationMode="Recycling"
                                        ScrollViewer.CanContentScroll="False">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <VirtualizingStackPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <ContentControl Content="{Binding}">
                                                </ContentControl>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                        <ItemsControl.ItemContainerStyle>
                                            <Style>
                                                <Setter Property="FrameworkElement.Visibility" Value="{Binding Visibility}"/>
                                                <Setter Property="FrameworkElement.Margin" Value="0 0 0 3"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                        <Setter Property="FrameworkElement.Margin" Value="0 0 0 1"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ItemsControl.ItemContainerStyle>
                                        <ItemsControl.Style>
                                            <Style>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                        <Setter Property="FrameworkElement.Margin" Value="3 0" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ItemsControl.Style>
                                    </ItemsControl>
                                </Expander>
                            </StackPanel>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                    <Setter Property="Margin" TargetName="MembersItemsControl" Value="3 0" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
            <!-- Region Template -->
            <DataTrigger Binding="{Binding DataTemplateType}" Value="Region">
                <Setter Property="ContentTemplate">
                    <Setter.Value>
                        <DataTemplate>
                            <Expander
                                IsExpanded="{Binding IsExpanded}"
                                Opacity="{Binding Opacity}"
                                HorizontalAlignment="Stretch">
                                <Expander.Header>
                                    <StackPanel Orientation="Horizontal">
                                        <vs:Image
                                            Source="{Binding Moniker}"
                                            Width="16"
                                            Height="16"
                                            Margin="0 0 3 0"
                                            VerticalAlignment="Center" />
                                        <TextBlock
                                            Text="{Binding Name}"
                                            FontSize="{Binding FontSize}"
                                            VerticalAlignment="Center" />
                                    </StackPanel>
                                </Expander.Header>
                                <ItemsControl
                                    x:Name="MembersItemsControl"
                                    ItemsSource="{Binding Members}"
                                    Margin="3"
                                    VirtualizingPanel.IsVirtualizing="True"
                                    VirtualizingPanel.VirtualizationMode="Recycling"
                                    ScrollViewer.CanContentScroll="False">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <VirtualizingStackPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <ContentControl Content="{Binding}">
                                            </ContentControl>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style>
                                            <Setter Property="FrameworkElement.Visibility" Value="{Binding Visibility}"/>
                                            <Setter Property="FrameworkElement.Margin" Value="0 0 0 3"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                                    <Setter Property="FrameworkElement.Margin" Value="0 0 0 1"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                </ItemsControl>
                            </Expander>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding CodeDocumentViewModel.UseCompactMode}" Value="True">
                                    <Setter Property="Margin" TargetName="MembersItemsControl" Value="3 0" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </Setter.Value>
                </Setter>
            </DataTrigger>
        </Style.Triggers>
    </Style>

</ResourceDictionary>
